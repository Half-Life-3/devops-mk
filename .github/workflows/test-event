name: test event

on: 
  schedule:
  - cron: 0 12 * * 1

jobs:
  test_event:
    name: test event
    runs-on: ubuntu-latest
    steps:
      - name: test-event
        uses:
        with: 
          assignees: "michael"
          pinned: true
        env:
          GITHUB_TOKEN : ${{secrets.GITHUB_TOKEN}}
